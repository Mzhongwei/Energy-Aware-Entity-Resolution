### Mandatory
mode: "bert-training-evaluation"
version_name: "spa"

# bert mode, source data
# NOTE: these absolute paths assume the container mounts pipeline-data-claim at /data
# (see k8s/pvc-manifests/pvc-data.yaml and the "pipeline-data" volume in
# k8s/pipeline/batch/pipeline.yaml / k8s/pipeline/incremental/*.yaml). If that mountPath
# ever changes, these paths must be updated to match -- nothing checks this alignment
# automatically. The PVC itself is populated separately by k8s/scripts/sync-data-pvc.sh,
# which shares the same PVC/directory as config-embedding.yaml's data_source_A/B (same
# underlying dataset, different files within it -- see sync-data-pvc.sh's own gap: it only
# verifies tableA/tableB/matches.txt exist locally before syncing, not train/test/valid.csv).
trainset_path: "/data/exp_datasets/4-1_dirty_dblp_acm/train.csv"
testset_path: "/data/exp_datasets/4-1_dirty_dblp_acm/test.csv"
evalset_path: "/data/exp_datasets/4-1_dirty_dblp_acm/valid.csv"

# STATE MANAGEMENT
state_management:
# bert mode
  bert-dir: "/app/data/bert"
# incremental mode. if execute several config files successively, the embedding_model-name (or version name) can not be the same
  graph-dir: "/app/data/graph"
  graph-name: ""
  embedding-dir: "/app/data/embedding"
  embedding_model-name: ""
  feature_index-dir: "/app/data/index"
  feature_index-name: ""
  predicted_match-dir: "/app/data/predicted"
  predicted_match-name: ""

### Optional
# pipeline
normalization:

graph_construction:

random_walk:

embedding_training:

bert_training:

cg_feature_extraction:

feature_index_construction:

candidate_enumeration:

calculating_similarity:

decision_making:

bert_inference:

evaluation:

b_evaluation:
